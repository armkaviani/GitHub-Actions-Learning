name: myFirstWorkflow
# To get trigger on every push in this repo in any branch
on: push

jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    steps:
      - name: Runing Sigle command in JOB1
        run: echo "Hi this is my first GitHub workflow"
      - name: Checking OS Release for GitHub Hosted Runner
        run: cat /etc/os-release
      - name: Calling Sleep
        run: sleep 20
  job2:
    name: JOB2
    runs-on: ubuntu-latest
    steps:
      - name: Checking the Hostname for Runner
        run: hostname
      - name: Running multiple commands
        run: |
          echo "Hi from Armaghan Kaviani"
          echo "Current time is 'date'"
          sleep 20
    
